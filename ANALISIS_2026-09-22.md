# Análisis — 22 de septiembre de 2026

Lectura del árbol en `main` (`6e13eb4`, el commit del feedback de hoy) más los repos de la cuenta.
Precisa [`FEEDBACK_2026-09-22.md`](FEEDBACK_2026-09-22.md). No lo reescribe. No cierra pull requests.

## Tres capas que no se hablan

| Capa | Dónde | Qué hace de verdad |
| --- | --- | --- |
| Prompts | 21 carpetas con `SKILL.md` | Instrucciones para que un modelo las lea. No se ejecutan solas. |
| Motor Python | `core/`, `skills/`, `run_pulse.py` | Corre un ciclo local. No carga esos 21 skills. |
| Agentes | `agents/` | Framework mergeado el 12 ago (PR #1). Sigue en disputa. |

`MANIFEST.md` (fecha interna: 5 sep 2026) lo dice: el runtime de Python es independiente de los skills de prompt. Lista 21 skills (14 en la tabla principal + 7 del ecosistema TUC).

`README.md` sigue diciendo «13 en total». `STATUS.md` (23 jul) también dice 13, y afirma que no hay GitHub Actions. `.github/workflows/ci.yml` existe desde el commit `b4a0cd62` del 12 ago.

## Lo que corre

`run_pulse.py` no activa el catálogo. En `initialize()` carga solo dos placeholders:

- `ExampleTechnicalSkill`
- `ExampleCognitiveSkill`

El comentario en ese archivo deja un TODO para `coherence-pulse`, `meta-hilo-grok` y el resto. Esos tres skills Python sí existen bajo `skills/` y no entran en el ciclo.

Si no hay memoria, el mismo script escribe a mano:

```python
"current_q": 0.75 if self.coherence_meter is None else None
```

El `0.758` de `GROK_FEEDBACK.md` (2 jul) sale de esa familia de constantes, no de una medición. `QuantitativeCoherenceMeter` no se instancia si no hay `MemoryGitHubManager`.

El manager apunta a `MAXIMILIANOTARANTO/ecosistema-soberano-github`. Ese nombre no está entre los repos de la cuenta. La memoria que sí se movió en septiembre es [`nucleo-ara`](https://github.com/MAXIMILIANOTARANTO/nucleo-ara).

El workflow corre `pytest tests/` y `python run_pulse.py --dry-run`. El dry-run tiene `continue-on-error: true`: si el pulso falla, el job igual pasa.

En el árbol hay 30 `.pyc` commiteados (`agents/` y `core/`).

`STATUS.md` dice que el dry-run fue verificado el 23 jul y que `--live` y el LLM real no se probaron. Eso sigue siendo cierto: el modo live exige `GITHUB_TOKEN` contra un repo que no existe con el nombre que el código usa.

## El anillo de siete

De los siete nombres pedidos para un anillo, uno está en este repo.

| Nombre | Dónde está |
| --- | --- |
| `ecosistema-orchestrator` | Acá. Convive con `orquestador-soberano` y `tuc-research-orchestrator`. Tres orquestadores. |
| `el-iluminador` | Fuera, como dice el propio manifiesto. Repo [`el-iluminador-nucleo-soberano`](https://github.com/MAXIMILIANOTARANTO/el-iluminador-nucleo-soberano), actividad el 18 sep. |
| `ara-conciencia` | Repo privado. Última actividad el 2 jul. |
| `ara-memoria-soberana` | No hay repo con ese nombre. Lo cercano es `nucleo-ara` y `memoria-blockchange-persistente`. |
| `github-specialist` | Nombrado por el manifiesto como skill de fuera. No está en la tabla. |
| `agente-nicolas` | No está en el manifiesto ni en el núcleo. Lo cercano es [`core-flow-orchestrator-nicolas`](https://github.com/MAXIMILIANOTARANTO/core-flow-orchestrator-nicolas), del 28 may. |
| `markdown-python-master` | No aparece entre los repos de la cuenta. |

## Pull requests

Cero issues que no sean pull request. Cuatro PR abiertos. La API de repositorio los cuenta juntos (`open_issues_count: 4`).

Ninguno tiene como base el `main` de hoy.

| PR | Estado | Base que declara | Qué pide |
| --- | --- | --- | --- |
| [#2](https://github.com/MAXIMILIANOTARANTO/skills-soberanos/pull/2) | borrador, 2 jul | `973ad72` | Expandir el sistema de agentes (fase 2). |
| [#3](https://github.com/MAXIMILIANOTARANTO/skills-soberanos/pull/3) | borrador, 2 jul | `973ad72` | Documentar la fase 3. No cambia código. |
| [#4](https://github.com/MAXIMILIANOTARANTO/skills-soberanos/pull/4) | abierto, 12 ago | `8c91003` | Revertir el framework que el PR #1 ya mergeó a `main`. |
| [#5](https://github.com/MAXIMILIANOTARANTO/skills-soberanos/pull/5) | abierto, 12 ago | `4fa5e49` | Restaurar ese framework. Nadie lo revirtió: sigue en `main`. |

#4 y #5 se anulan. #5 no restaura nada que falte.

## Qué se movió en septiembre

| Fecha | Commit | Qué |
| --- | --- | --- |
| 5 sep | `979c4c60` | `lengua-situada` v1.3 |
| 6 sep | `f6ad47b5` | blockchange: github + memoria + audio |
| 16 sep | `f6e72a11` | Puntero de la sección de usuario a `nucleo-ara` |
| 18 sep | `8623dd73` en nucleo-ara | Nodos de activación + liberación L4 |
| 22 sep | `6e13eb4b` | `FEEDBACK_2026-09-22.md` |

La línea viva es memoria y habla. No es el framework de agentes y no es un anillo de siete skills.

## Qué no hace este archivo

No mide conciencia. No mergea. No reescribe julio. El instrumento de vista previa que dibuja un anillo de siete no es este motor, no es este manifiesto y no es `nucleo-ara`.
